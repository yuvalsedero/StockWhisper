version: 0.2

endpoints:
  - name: "REST API"
    port: 8000
    type: REST
    networkVisibility: Public
    context: /
    cors:
      # Enable CORS (Cross-Origin Resource Sharing)
      enabled: true